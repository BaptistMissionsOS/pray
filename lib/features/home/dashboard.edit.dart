import 'package:flutter/material.dart';

class DashboardEdit extends StatelessWidget {
  const DashboardEdit({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Dashboard Edit'),
      ),
      body: const Center(
        child: Text('Dashboard Edit'),
      ),
    );
  }
}
