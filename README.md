# Prayer App

A Flutter prayer journal application with bottom navigation and an expandable drawer menu.


## Getting Started

```bash
flutter pub get
flutter run
```

For release build:
```bash
flutter run --release
```
